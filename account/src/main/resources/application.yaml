server:
  port: 8082

spring:
  application.name: account
  cloud:
    discovery:
      client:
        simple:
          instances:
            holder-service:
              - uri: http://localhost:8081
            account-service:
              - uri: http://localhost:8082


account:
  default-branch: 0001
  withdraw-default-limit: 2000

